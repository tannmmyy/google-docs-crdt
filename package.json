{
  "name": "google-docs-crdt",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack real-time collaborative Google Docs-style rich text editor with CRDT",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build",
    "start": "node server/dist/server.js",
    "dev": "npm --prefix server run dev",
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "test:crdt": "npm --prefix server run test:crdt"
  }
}
